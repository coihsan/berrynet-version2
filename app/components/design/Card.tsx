interface Props {
  className: string;
  title: string;
  description: string;
  icon: JSX.Element;
}
export default function Card({ className, title, description, icon }: Props) {
  return <div></div>;
}
